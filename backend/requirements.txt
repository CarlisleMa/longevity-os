# LongevityOS backend — runtime deps (representative mode + live coach).
# This is all the running app needs; keep it lean for fast cloud builds.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9      # file uploads
anthropic>=0.40              # live multi-agent coach (set ANTHROPIC_API_KEY; COACH_MODEL optional)

# Scoring-live extras (frozen aging-clock scoring on uploaded data) are in
# requirements-scoring.txt — not needed to run the app or the coach.
